# 2026-06-06

## PDCA追加: exp031/exp032

### Plan

- SDSS17外部元データを直接照合ではなく、aggregate class prior、prototype距離、count特徴としてLightGBMへ追加する。
- そのbaseを現行best stackへ追加し、CVが明確に改善するか確認する。

### Do

- `scripts/run_lgbm_sdss_prior.py` を追加。
- `exp031_lgbm_sdss_prior_fast` を3-fold LightGBMで実行。
- `exp032_oof_stacker_with_sdss_prior` で `exp031` を `exp030` stackへ追加。
- `AGENT.MD`、`CLAUDE.MD`、`AGENTS.md` に pure PyTorch policy を追記。

### Check

- `exp031`: CV balanced_accuracy 0.959431。単体として弱い。
- `exp032`: CV balanced_accuracy 0.966223。`exp030` との差分は約 +0.000001。
- 改善幅が極小で、かつ `exp032` はmultiplier search込みのため、API提出基準を満たさない。

### Act

- `exp032` はno_submit。
- SDSS特徴は、少数の安定prior/prototypeへ絞る、fold-safe target encoding/feature selectionを入れる、または既存強モデルに限定的に追加する方針へ修正する。
- torch系モデルは純粋なPyTorch実装のみで継続する。

## PDCA追加: exp033/exp034

### Plan

- `exp031` の失敗を受け、SDSS17特徴をcoarse prior/prototypeのみに絞ったLightGBM baseを作る。
- 現行best stackへ追加し、CVが明確に改善するか確認する。

### Do

- `scripts/run_lgbm_sdss_stable.py` を追加。
- `exp033_lgbm_sdss_stable` を3-foldで実行。
- `exp034_oof_stacker_with_sdss_stable` でstack効果を確認。

### Check

- `exp033`: CV balanced_accuracy 0.963558。`exp031` より改善したが、強いbaseには届かない。
- `exp034`: CV balanced_accuracy 0.966206。`exp030/032` 未満。

### Act

- `exp034` はno_submit。
- SDSS aggregate特徴はいったん優先度を下げ、純粋PyTorch tabular baseまたはGPU XGBoost/CatBoost強化へ進む。

## PDCA追加: exp035/exp036/exp037

### Plan

- ユーザー指定どおり、外部tabular NN packageを使わず純粋PyTorchのMLP baseを作る。
- tree model stackへNN系の誤差多様性を追加し、CV改善があるか確認する。

### Do

- `scripts/run_torch_tabular_mlp.py` を追加。
- 初回 `exp035` は全NaN列によりlossがNaN化したため、全NaN feature drop と `nan_to_num` を追加して再実行。
- `exp036_oof_stacker_with_torch_mlp` でsklearn logistic stackを確認。
- `exp037_torch_logit_stacker_with_mlp` で純PyTorch logit stack confirmationを実行。

### Check

- `exp035`: CV balanced_accuracy 0.950964。単体は弱い。
- `exp036`: CV balanced_accuracy 0.966349。記録上new diagnostic best。
- `exp037`: CV balanced_accuracy 0.966175。`exp036` の改善は純PyTorch stackerでは再現せず。

### Act

- `exp036` はno_submit。改善は小さく、stacker/threshold依存の可能性がある。
- 次はMLP baseのcalibration/architecture改善、またはGPU tree variant強化を優先する。

## PDCA追加: exp038/exp039/exp040/exp041

### Plan

- `exp036` のMLP diversityにSDSS系弱baseを組み合わせ、弱baseの相補性を検証する。
- confirmationとして純粋PyTorch logit stackerでも同じbase setを評価する。

### Do

- `exp038`: MLP + `exp031` + `exp033` を追加したsklearn logistic stack。
- `exp039`: 同じbase setの純粋PyTorch logit stack。
- `exp040`: MLP + `exp031` の切り分け。
- `exp041`: MLP + `exp033` の切り分け。

### Check

- `exp038`: CV balanced_accuracy 0.966420。new diagnostic best。
- `exp039`: CV balanced_accuracy 0.966322。`exp030/032` は超えるが `exp038` ほどではない。
- `exp040`: CV balanced_accuracy 0.966407。`exp038` 未満。
- `exp041`: CV balanced_accuracy 0.966379。rawは強いが `exp038` 未満。

### Act

- `exp038` はbest diagnosticとして保持するが、API提出はまだ保留。
- 次は別split meta-stackまたはcalibrationで `exp038` の安定性を確認する。

## PDCA追加: exp042/exp043

### Plan

- `exp038` のbase setを別meta split seedで評価し、seed 2026固有の上振れかを確認する。

### Do

- `scripts/stack_oof_meta.py` に `--n-folds` と `--fold-seed` を追加。
- `exp042_oof_stacker_mlp_sdss_seed777` を実行。
- `exp043_oof_stacker_mlp_sdss_seed31415` を実行。

### Check

- `exp042`: CV balanced_accuracy 0.966398。threshold-freeで強い。
- `exp043`: CV balanced_accuracy 0.966354。`exp030/032` は上回る。
- `exp038` base setは別splitでも改善を維持した。

### Act

- seed単体では提出しない。
- 次は `exp038/042/043` のseed-averaged stack candidateを作り、CVと提出根拠を確認する。

## PDCA追加: exp044

### Plan

- `exp038/042/043` のseed平均candidateを作り、seed選択の任意性を下げたsubmission候補を評価する。

### Do

- `scripts/average_oof_proba.py` を追加。
- `exp044_seed_average_stacker` を実行。

### Check

- Raw CV balanced_accuracy: 0.966366。
- Thresholded CV balanced_accuracy: 0.966383。
- `exp038` 0.966420 と `exp042` 0.966398 を下回る。

### Act

- `exp044` はno_submit。
- seed平均は安定化には効くが、best CV candidateではない。次は `exp038` のcalibrationか、MLP/tree base改善を優先する。

## Plan

- 調査により、LB/PB 1位に必要な手法と必要情報を整理する。
- 過去・類似Kaggle解法と論文から、次の実装優先順位を決める。

## Do

- Kaggle APIでleaderboardとsubmission historyを確認。
- `cdeotte/gpu-logistic-regression-stacker`, `philippsinger/tabpfn-3-stacker`, `yekenot/ps-s6-e6-realmlp-pytorch`, `evgendvorkin/single-lightgbm-lb-0-96728` をpullして確認。
- `cdeotte/lgbm-v3-for-s6e6`, `cdeotte/xgb-v5-for-s6e6`, `cdeotte/cat-v3-for-s6e6` をpullして確認。
- `Winning_Strategy_Research_2026-06-06.md` を作成。
- PyTorch CUDA wheel (`torch 2.5.1+cu121`) をvenvに導入。
- `scripts/stack_torch_logit.py` を実装し、`exp028_torch_logit_stacker` を実行。
- `scripts/run_catboost_v3_style.py` を実装し、`exp029_catboost_v3_style` を実行。
- `exp030_oof_stacker_with_cat_v3` でCatBoost v3-style baseをstackへ追加。

## Check

- Current public #1: 0.97127。
- Best external-public-blend LB: `exp008`, 0.97074。
- Best confirmed self LB: `exp020`, 0.96642。
- Best self CV: `exp024`, 0.966112, LB未確認。
- 上位公開notebookは、多様なOOF/test predsをlogit化し、GPU logistic regressionやTabPFN-3でstackする構成。
- `exp028_torch_logit_stacker`: pure PyTorch, CV balanced_accuracy 0.965935。`exp024` 0.966112未満のためno_submit。
- `exp029_catboost_v3_style`: CV balanced_accuracy 0.964213。過去CatBoostより大幅改善。
- `exp030_oof_stacker_with_cat_v3`: CV balanced_accuracy 0.966222。new best CVだが、改善幅+0.000110のためno_submit。

## Act

- 次の実装優先順位は、CDeotte型logit stacker、SDSS17 prior/prototype features、CatBoost v3-style native categorical/original-weight base、RealMLP/TabM/TabPFN系base。
- submission limit reset後、まず`exp024`のLBを確認する。
- 純PyTorch stackerは実装済みだが、現baseではCV未達。次はmeta-modelよりbase model追加を優先する。
- CatBoost v3-style baseはstackに効いた。次はSDSS17 prior/prototype featuresまたはCatBoost v3-styleの5-fold/強化版を検討する。
